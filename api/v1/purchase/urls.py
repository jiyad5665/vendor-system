from django.urls import path

from purchase import views

app_name="purchase"

urlpatterns=[
    path('',views.index, name="index"),
    path('',views.index, name="index"),
    path('',views.index, name="index"),
    path('',views.index, name="index"),
    path('',views.index, name="index"),
    
]

